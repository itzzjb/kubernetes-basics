# This is the file that will contain all the secrets related to mongodb
apiVersion: v1
kind: Secret
metadata:
  name: mongo-secrets
# Opaque is the most common type of secret
type: Opaque
data:
  # When we use opaque, we need to encode the values in base64
  # We can use a online tool to encode the values
  MONGO_USER: bW9uZ291c2Vy
  MONGO_PASSWORD: bW9uZ29wYXNzd29yZA==
